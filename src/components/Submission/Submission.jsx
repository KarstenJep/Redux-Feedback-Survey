function Submission() {
    return (
             <div>
                <button className="next" onClick={handleNext}>Leave New Feedback</button>
            </div>
    )
}

export default Submission;